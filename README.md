# docker-hosts-vnc - браузер в контейнере  

Для изменения содержимого файла /etc/hosts

Если нет возможности изменить содержимое файла /etc/hosts, но очень нужно, можно воспользоваться контейнером с браузером.

## ✨ Преимущества
-  **все запускается несколькими командами**
-  **не требуются права SUDO для внесения DNS записей**
-  **не требуются права на DNS сервере**


## 🛠️ Последовательность
- **Склонировать репозиторий**
```bash
git clone https://github.com/adjuster2004/docker-host-vnc/
cd docker-host-vnc
```

- **Создаем директорию для данных**
```bash
mkdir -p /data
```

- **Даем права на выполнение**
```bash
chmod +x update_hosts.sh entrypoint.sh
```

- **Собираем образ**
```bash
DOCKER_BUILDKIT=0 docker build -t hosts-manager -f Dockerfile.vnc .
```

- **Запускаем контейнер**
```bash
docker run -d \
      --name hosts-manager \
      -p 5000:5000 \
      -p 5900:5900 \
      -p 6080:6080 \
      -v $(pwd)/data:/data \
      --shm-size=1gb \
      --privileged \
      hosts-manager
```

- **Открываем браузер на рабочем компьютере**
и указываем в адресной строке 
```
http://localhost:6080/vnc.html
```

<img width="642" height="475" alt="image" src="https://github.com/user-attachments/assets/d5360c7d-3dec-4c9e-a9cf-71b586d61fe4" />


- **Потребуется авторизоваться**
пароль есть в файле Dockerfile.vnc = **admin123**
```
ENV DEBIAN_FRONTEND=noninteractive \
    DISPLAY=:99 \
    VNC_PORT=5900 \
    NOVNC_PORT=6080 \
    VNC_PASSWORD=admin123 \
    RESOLUTION=1280x720
```

- **Содержимое файла /etc/hosts можно менять без захода в контейнер**
```
http://localhost:5000/
```
<img width="532" height="417" alt="image" src="https://github.com/user-attachments/assets/e231e27a-ba05-428a-90a3-b8c799b35c0b" />

 
## 📄 Лицензия
Этот проект распространяется под лицензией **MIT**.

Copyright (c) 2025 Sergey S @adjuster2004

Подробности в файле [LICENSE](LICENSE).
